<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.franciswairegi.weatherforecast">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".WeatherForecastMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".WeatherForecastCityListActivity"
            android:parentActivityName=".WeatherForecastMainActivity">
        </activity>
        <activity
            android:name=".WeatherForecastDetailPagerActivity"
            android:parentActivityName=".WeatherForecastMainActivity">
        </activity>
    </application>

</manifest>