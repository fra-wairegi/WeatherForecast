<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">-->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/green</item>
        <item name="colorPrimaryDark">@color/dark_green</item>
        <item name="colorAccent">@color/gray</item>
    <!--    <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>-->
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

<!--    <style name="AppThemeToggle" parent="@android:style/Widget.Material.Light.Button.Toggle">
        <item name="colorAccent">@color/gray</item>
        <item name="android:colorControlNormal">@color/light_green</item>
        <item name="android:colorControlHighlight">@color/light_green</item>
        <item name="android:colorButtonNormal">@color/dark_green</item>
    </style>-->

    <style name="AppThemeToggle" parent="@android:style/Widget.Material.Light.Button.Toggle">
        <item name="colorAccent">@color/green</item>
        <item name="android:colorControlNormal">@color/green</item>
        <item name="android:colorControlHighlight">@color/light_green</item>
        <item name="android:colorButtonNormal">@color/green</item>
    </style>

</resources>
